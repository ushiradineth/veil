{
  "agents": ["codex", "claude", "firecrawl"],
  "strategies": ["mcp_transport"],
  "notes": [
    "This file documents the normalized benchmark matrix contract.",
    "Each agent/strategy cell must run the same scenario prompt and query against the same workspace.",
    "Current bench-suite defaults include optional firecrawl competitor cells.",
    "firecrawl MCP rows can remain unsupported when MCP transport wiring or API credentials are unavailable."
  ]
}
